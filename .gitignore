# ── Rust 빌드 ──────────────────────────────────────────────
src-tauri/target/

# ── FFmpeg 바이너리 (플랫폼별 다운로드, 커밋 제외) ─────────
src-tauri/binaries/

# ── Node.js / pnpm ─────────────────────────────────────────
node_modules/
.pnpm-store/

# ── 빌드 결과물 ────────────────────────────────────────────
dist/
dist-ssr/

# ── TanStack Router 자동 생성 파일 ──────────────────────────
src/routeTree.gen.ts

# ── Vite 임시 파일 ─────────────────────────────────────────
vite_out.txt
vite_err.txt
*.local

# ── 미디어 임시 파일 ───────────────────────────────────────
.video-editor-temp/
._*
reference/

# ── 환경변수 ───────────────────────────────────────────────
.env
.env.local
.env.*.local

# ── TanStack 캐시 ──────────────────────────────────────────
.tanstack/

# ── 에디터 ─────────────────────────────────────────────────
.idea/
*.swp
*.swo

# ── OS ─────────────────────────────────────────────────────
.DS_Store
Thumbs.db
desktop.ini

# rust-analyzer/CodeLLDB의 CARGO_TARGET_DIR가 리터럴 경로로 잘못 생성된 경우 차단
# 예) {env:HOME}/..., ${env:HOME}/..., src-tauri/{env:USERPROFILE}/...
/{env:HOME}/
/{env:USERPROFILE}/
/${env:HOME}/
/${env:USERPROFILE}/
src-tauri/{env:HOME}/
src-tauri/{env:USERPROFILE}/
src-tauri/${env:HOME}/
src-tauri/${env:USERPROFILE}/

# Playwright
/e2e/fixtures/
/test-results/
/playwright-report/
/blob-report/
/.playwright/
