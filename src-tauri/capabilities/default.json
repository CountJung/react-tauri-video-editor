{
  "$schema": "https://schema.tauri.app/config/2",
  "identifier": "default",
  "local": true,
  "windows": ["main"],
  "permissions": [
    "core:default",
    "core:window:allow-close",
    "core:window:allow-destroy",
    "shell:allow-execute",
    "shell:allow-open",
    "fs:default",
    "dialog:default"
  ]
}
